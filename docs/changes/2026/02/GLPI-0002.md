# GLPI-0002

## Release
- Versão: v0.1.0
- Data: 2026-02-12
- Versão anterior: N/A

## Referências
- Autor: brenokf
- Aprovadores: Nenhum aprovador registrado
- Commit anterior (base): https://github.com/brenokf/SmartSimulate/commit/85e278b58b2829316220f5873c7062da24b0e0b2
- Commit novo (head): https://github.com/brenokf/SmartSimulate/commit/247c5f409e038482e5c7b513a02e2ee97dde6959
- Comparação (anterior → novo): https://github.com/brenokf/SmartSimulate/compare/85e278b58b2829316220f5873c7062da24b0e0b2...247c5f409e038482e5c7b513a02e2ee97dde6959

## Insights automáticos da alteração
- Resumo do diff:  7 files changed, 920 insertions(+), 56 deletions(-)

### Arquivos com maior impacto
- .github/workflows/change-control.yml (311 linhas alteradas)
- docs/app.js (255 linhas alteradas)
- App.tsx (168 linhas alteradas)
- docs/styles.css (120 linhas alteradas)
- docs/index.html (60 linhas alteradas)
- docs/releases.json (37 linhas alteradas)
- .github/pull_request_template.md (25 linhas alteradas)

## Relatório de alterações
- .github/pull_request_template.md
- .github/workflows/change-control.yml
- App.tsx
- docs/app.js
- docs/index.html
- docs/releases.json
- docs/styles.css

## Métodos/Funções/Classes impactadas
- @media (max-width: 1100px) {
- @media (max-width: 700px) {
- const buildKpis = () => {
- const da = (a.date || '');
- const db = (b.date || '');
- const fmtDate = (value) => {
- const getRepoRootUrl = (release) => {
- const getVisibleReleases = (query = '') => {
- const groupByMonth = (items) => {
- const markdownToText = (md = '') => {
- const monthLabel = (value) => {
- const renderList = (query = '') => {
- const safeArray = (v) => (Array.isArray(v) ? v : []);
- dedupe_lines () {
- extract_section () {
- for (let index = 0; index < pairsToLoad.length; index += PAIRS_PER_REQUEST) {
- if (!payload || payload.status === 404) {
- if (!release?.docs?.releaseDocPath) {
- if (!response.ok && hasApiToken) {
- if (!response.ok) {
- if (cachedEntry) {
- if (cachedEntry?.rates) {
- if (currencyCode) {
- if (filteredReleases.length === 0) {
- if (popularAvailable.length > 0) {
- tolower($0) ~ tolower("^#+[[:space:]]*" title "[[:space:]]*$") {found=1; next}
- } else if (cachedEntry) {

## Commits no intervalo
- 247c5f4 feat: Add new styles for dark mode and enhance UI components
- 1adcff0 feat: Add initial release information in releases.json
- 26f6802 feat: Add initial release hub HTML structure with navigation and details sections
- 79363ac feat: Implement release management UI with search and KPI display
- be0de1e fix: Enhance change control workflow by adding PR reviewers collection and updating release catalog
- 90b8287 fix: Improve PR title validation regex and enhance error messaging
- 8f7101f fix: Validate PR title format and enhance change report with diff summary and top touched files
- 9b83048 feat: Add pull request template for improved contribution guidelines
- 4a5ceb7 fix: Update currency pairs handling and improve API fetching logic
- 569717c fix: Enhance change control workflow with improved metadata extraction and documentation generation

## Pontos críticos para teste (Como validar)
Não informado no formato de seção markdown; verificar corpo do PR.

## Impacto
Não informado no formato de seção markdown; verificar corpo do PR.

## Rollback
Não informado no formato de seção markdown; verificar corpo do PR.
